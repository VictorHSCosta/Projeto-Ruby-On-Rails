<div id="main" class="text-gray-800 w-4/5 mx-auto p-8 bg-white rounded-lg shadow-md m-20">
  <h1 class="text-2xl text-green-700 mb-6 p-4">Deletar Item</h1>
  
  <p class="text-green-500"><%= notice %></p>

  <p class="my-4 text-xl">
    <strong>Voce tem certeza que vai <span class="text-green-700">deletar</span> o item:</strong>
    <%= @item.name %>
    <strong> ?</strong>
  </p>

  <div id="botoes" class="flex md:justify-between pt-6 flex-col md:flex-row">
    <%= button_to "Deletar item", list_item_path(@list, @item), method: :delete, data: { confirm: "Tem certeza?" }, class: "bg-primary text-white rounded-full py-2 px-6 cursor-pointer transition duration-300 ease-in-out hover:bg-blue-700 hover:transform hover:scale-105 w-[95%] md:w-auto" %>  
    <%= link_to "Voltar para items", list_path(@list), class: "bg-primary text-white rounded-full py-2 px-6 cursor-pointer transition duration-300 ease-in-out hover:bg-blue-700 hover:transform hover:scale-105 w-[95%] md:w-auto md:m-0 mt-4" %>
  </div>
</div>
